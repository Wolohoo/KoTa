#version 130

in vec2 UV;
uniform sampler2D myTextureSampler; //Texture data

out vec3 color;

void main()
{
color = texture(myTextureSampler, UV).rgb;  // tämä funktio ei toimi ilman 130 tukee siksi muutettiin 
}